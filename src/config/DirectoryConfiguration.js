const PUBLIC = './public';
const PRINTED = PUBLIC + '/printed';
const PRINTING = PUBLIC + '/printing';

module.exports = Object.freeze({
    PUBLIC: PUBLIC,
    PRINTED: PRINTED,
    PRINTING: PRINTING,
});